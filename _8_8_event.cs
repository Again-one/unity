using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class _8_8_event : MonoBehaviour {
    public GameObject obj;
	// Use this for initialization Ctrl+alt+空格
	void Start () {
        //启动和刷新onStart，onUpdate之类
        /**
         * 物理：OnTriggerStay
         * onTriggerEnter()
         */
        //输入：OnMouseExit
        //渲染：OnGUI
        //对象：OnEable OnDisable OnDestroy OnLevelWasLoaded
        //场景：Scene
        //程序：:OnApplicationPause()
        //网络：OnPlayerConnected
        //动画：OnAnimatorMove()
        //声音：OnAudioFilterRead()
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
